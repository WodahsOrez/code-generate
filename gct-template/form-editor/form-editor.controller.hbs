import { EditorController } from '@gct/runtime';
import { I{{pascalCase}}, I{{pascalCase}}Controller } from '../../interface';

/**
 * {{chineseText}}编辑器
 * @author lingxiaoming
 * @date 2024-07-17 06:03:56
 * @export
 * @class {{pascalCase}}Controller
 * @extends {EditorController<I{{pascalCase}}>}
 * @implements {I{{pascalCase}}Controller}
 */
export class {{pascalCase}}Controller
  extends EditorController<I{{pascalCase}}>
  implements I{{pascalCase}}Controller
{
  protected override init(): void {
    super.init();
    // 写自定义的初始化逻辑
  }
}
